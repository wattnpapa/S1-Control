# Auftrag M1.3 — `@s1/domaene` in voller Breite

Stand: 2026-09-10 · Arbeitspaket M1.3 aus [05-UMSETZUNGSPLAN.md](../../v2/05-UMSETZUNGSPLAN.md) · Status: **abgeschlossen**

> **Bearbeitungsstand 2026-09-10.** Stufen 1 bis 3 liegen vor: Zustand,
> Katalog und Fold über alle Ereignisarten, einschließlich der beiden
> strukturellen, der Barriere `EinsatzArchiviert`, Undo U1 bis U6 und
> `KorrekturVon`; Stufe 4 (Kennzahlen K1 bis K30) und Stufe 5 (P1 bis P7)
> ebenso. Der Abschlussbericht steht in
> [M1-abschlussbericht.md](../berichte/M1-abschlussbericht.md).
> Gebaut wird gegen die zweiunddreißigste Fassung von
> [KONZEPT-EREIGNISSE.md](../../v2/konzepte/KONZEPT-EREIGNISSE.md). Das Konzept
> ist noch nicht freigegeben; weicht der Code von ihm ab, ist das ein Befund und
> keine Auslegung. Beim Bauen umgefallen ist genau eine Regel: §2.2a erzeugte
> `vorherPasstNicht` auch dort, wo der Gewinner denselben Wert setzte wie die
> verdrängte Beobachtung, und widersprach damit T66. Der Vorbehalt aus §2.3 gilt
> jetzt an beiden Stellen.

Aufträge werden hier abgelegt, damit nachvollziehbar bleibt, wogegen ein Paket gebaut wurde.

---

## DoD aus dem Umsetzungsplan

`@s1/domaene`: Zielmodell (Einsatz, Abschnitt, EinsatzEinheit, Meldung, Fahrzeug, Person,
Auftrag, Anforderung, Dienstposten, ETB) mit allen Feldern aus Zieldatenmodell §3; Fold für
alle Ereignistypen; Kennzahlen (Summen je Bereich, Organisation, Status, Schicht; Gesamtstärke
ohne Anmarsch) mit den Excel-Formeln.

**Abnahme: Property-Tests grün, einschließlich P4 (Summenerhaltung) und P6 (monotone
Zustandsmaschine); Kennzahlen gegen von Hand gerechnete Werte der Referenzlage.**

P4 und P6 stehen seit M0.2 als übersprungene Tests in `eigenschaften.test.ts`, mit der
Begründung, dass die dafür nötigen Ereignisarten fehlten. Dieses Paket löst sie ein; ein
Ersatztest unter demselben Namen wäre nach Auflage 18 kein Grün.

## Zuschnitt in fünf Stufen

Jede Stufe ist ein eigener Commit und lässt `tsc -b`, `eslint .` und `vitest run` grün zurück.

1. **Zustand** — `zustand.ts` auf `foldVersion 2`: `Beobachtung`, `Feld`, `Erstwert`,
   `VerworfeneAnlage`, die elf Entitätsarten, `schichtplan`, `archivierungen` an der Wurzel,
   `hinweise`, `unbekannt`, `wartend`, `verworfeneSchluessel` (Konzept §3.2).
2. **Katalog** — der vollständige Ereigniskatalog aus §5 mit zod-Schemata, `schemaVersion`
   je Nutzlastart, Upcaster-Kette und der Toleranz aus §3.7.
3. **Fold** — die Regeln aus §3 bis §7 als Mengenfunktion mit Rebase, einschließlich
   Auffangregel, Zyklusregel, Erstwert-Auswahl, Undo U1 bis U6 und der Barriere
   `EinsatzArchiviert`.
4. **Kennzahlen** — Summen je Bereich, Organisation, Status und Schicht sowie die
   Gesamtstärke ohne Anmarsch, gegen die Excel-Formeln.
5. **Eigenschaften** — P1 bis P7 nach §8 und §11.1, mit den Schranken gegen das Schein-Grün.

## Was dieses Paket nicht tut

* Keine Oberfläche, kein Electron. Die Kennzahlen sind Funktionen, keine Ansichten.
* Die Speicherschicht wird benutzt und nicht angefasst. Eine Lücke dort ist ein Befund.
* `KONZEPT-EREIGNISSE.md` wird nicht nachgebessert, um Code zu rechtfertigen. Fällt beim
  Bauen eine Regel um, wird sie im Konzept korrigiert und der Grund im Commit genannt.
* Der eeb-Adapter (`EinheitGemeldet` aus einem Bogen) ist M1.5.

## Kalibrierung der Prüfstandsgrößen (S10, §11.1 des Ereigniskonzepts)

§11.1 nennt die Zahlen ausdrücklich als Startwerte, „gegen die Laufzeit in der CI
zu kalibrieren". Gemessen am 2026-09-10, mit `vitest run` über alle Pakete
(rund 25 Sekunden Gesamtlaufzeit):

| Prüfung | §11.1 verlangt | gebaut | Grund der Abweichung |
|---|---|---|---|
| P1 | 500 Permutationen je Menge, 200 Mengen von 40 bis 120 Ereignissen | 300 Mengen von 6 bis 40 Ereignissen, je eine Permutation, dazu 300 Läufe über den Live-Pfad mit zufälligen Schüben | Eine Permutation je Menge über 300 Mengen deckt denselben Raum ab wie 500 über wenige; die Mengenlänge ist auf 40 begrenzt, weil P7 über **jeden** Schnitt läuft und quadratisch wächst |
| P2 | dieselben Mengen, jedes Ereignis doppelt | 200 Mengen, ganze Menge und einzelne Wiederholungen | — |
| P3 | dieselben Mengen, zwei Faltungen | 300 Mengen, verschiedene Ankunft und Schubgröße | — |
| P4 | 100 Mengen mit zwei bis fünf Vorgängen | 150 Mengen, rund 470 strukturelle Ereignisse insgesamt | — |
| P5 | alle Mengen aus P1 | 300 Mengen, beide Hälften | — |
| P6 | alle Permutationen und alle Präfixe je Menge | 400 Mengen über alle Präfixe, 150 über Permutationen | Alle Permutationen einer Menge von 40 Ereignissen sind nicht aufzählbar; geprüft wird eine gezogene je Menge |
| P7 | für jede Menge jeder Schnitt | 120 Mengen, jeder Schnitt in Ankunfts- **und** in HLC-Ordnung | Die zweite Ordnung steht nicht in §11.1 und ist der Betriebsfall: Ein Schnappschuss entsteht an einer HLC-Grenze |

Die Messgrößen, die den Prüfstand gegen das Schein-Grün absichern (Auflage 18),
werden mitgezählt und behauptet, nicht bloß gehofft: HLC-Gleichstände auf
demselben Feld, Konkurrenz zweier Clients auf demselben Feld, der Anteil
geprüfter statt übersprungener strukturureller Ereignisse bei P4 und die Zahl der
Mengen, in denen eine Anforderung überhaupt `EINGETROFFEN` erreicht.

## Prüffälle

§11 des Konzepts nennt 195 Fälle (T1 bis T195). Sie sind die Abnahmeliste dieses Pakets:
Zu jedem Fall gehört ein Test, und ein Fall ohne Test ist eine offene Stelle, keine
Auslegung. Die Zuordnung Testname → T-Nummer steht im jeweiligen Test.
