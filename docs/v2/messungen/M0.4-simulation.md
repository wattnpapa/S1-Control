# M0.4 — Konvergenz unter Störung

Protokoll des Arbeitspakets M0.4 aus `05-UMSETZUNGSPLAN.md` und Nachweis zu
den Auflagen 15 und 18 aus `03-MEILENSTEINE.md`.

Alles, was hier steht, ist an der **Simulation** gemessen, nicht am echten
Share. Die Messung am Synology-Gerät ist M0.5; dieses Dokument liefert ihr die
Zahlen, die ohne SMB bestimmbar sind, und benennt bei jeder, was noch offen
bleibt.

---

## 1 Was gebaut wurde

`s1 simuliere` in `packages/cli/src/simulation/`. Der Lauf hängt die
**feindliche Dateisystem-Schicht** an den Port `Dateisystem` aus
`@s1/speicher` (KONZEPT-SPEICHER.md §5.4.2, §6.2) und die virtuelle Uhr an die
Zeitquelle aus §8. Darüber merkt keine Schicht der Speicherschicht, welches
Dateisystem und welche Uhr laufen — das ist die Naht, die M0.3 dafür gebaut
hat.

```
s1 simuliere --clients 4 --kommandos 2000 --phasen 5 --startwert 20260904
```

Ohne Angaben läuft der **Abnahmeplan**: 4 Clients, 2.000 Kommandos, 5 Phasen,
alle Störungen. Exitcode 0 heißt bestanden, 1 heißt Mangel — Auflage 18
verlangt ein zählbares Kriterium, und ein Abnahmeschritt muss ohne
Textauswertung entscheidbar sein.

### 1.1 Die Störungen und wo sie herkommen

§9 (Zeile zu Auflage 15) teilt sie in zwei Gruppen, und die Trennung wird
eingehalten.

**Mit Konzeptregel — geprüft wird das festgelegte Verhalten:**

| Störung | Konzept |
|---|---|
| abgeschnittener Anhang auf dem Share | §5.4.1 („Was dort ankommt, ist ein Präfix dessen, was gesendet wurde") |
| Kill mitten im lokalen Append | §5.2, §8.1 |
| Partition | §8.3, §8.9 „vorübergehend" |
| entzogenes Schreibrecht | §8.9 „dauerhaft" |
| blockierender Aufruf | §8.4 |
| lokale Schreibstörung `ENOSPC`/`EACCES`/`EBUSY`/`EIO` | §8.8 |
| Uhrsprung, vorwärts und rückwärts | §3.2, §8.5 |
| Beschädigung einer Zeile in der Dateimitte | §8.2 |
| geklontes Profil | §4.5 Fall 2, §5.4.3 Ausgang C |

**Ohne Konzeptregel — geprüft wird allein, dass sie folgenlos bleiben:**
verzögerte Sichtbarkeit, `FileNotFound`-Cache (5 s), Verzeichnis-Cache (10 s),
Rename-Fehler `EPERM`. Für diese vier ist **kein** Verhalten erfunden worden.

**Eine Ausnahme, benannt:** Die verzögerte Sichtbarkeit gilt nicht für die
eigenen Share-Segmente des Schreibers. §6.6 („Wann die Lease-Annahme kippt")
legt fest, dass er zur Feststellung des Share-Endes nach §5.4.2 neu öffnet,
und die Recherche trägt, dass ein Datenlesezugriff ohne gültige Lease zum
Server durchgeht. Ihm ein zu frühes Dateiende zu zeigen prüfte nicht das
Verfahren, sondern Annahme A5 (§10) — und die misst M0.5 am Gerät.

### 1.2 Was gemessen wird

* **Konvergenzvergleich nach §7.6** nach jeder Ruhephase: das Paar aus
  Versionsvektor und `zustandsHash`, drei Ausgänge, Clients mit Quarantäne
  getrennt gemeldet (§8.6.1 Regel 3).
* **lokal ↔ Share je Client**: für die eigenen Segmente unter Bedingung 1 der
  Ruhephase byteweise Gleichheit. Für fremde Dateien ausdrücklich nicht (§5.5:
  geprüftes Präfix, ab einer Quarantänestelle kürzer); für die Datei einer
  aufgegebenen Kennung ebenfalls nicht — sie ist nach §4.5 Schritt 6 eine
  fremde, geprüft wird dort das Präfix.

---

## 2 Zwei Abweichungen von §7.6, die der Lauf erzwungen hat

Beide machen die Ruhephase **schwerer** erreichbar, nicht leichter. Sie sind
hier benannt, weil sie das Konzept berühren.

### 2.1 Bedingung 2 und 3 hängen am Fortschritt, nicht am Umsatz

§7.6 verlangt „0 Bytes geliefert". Eine Datei mit vorläufiger Quarantäne wird
nach §8.1 in **jedem** Takt-B-Durchlauf erneut geprüft, und dabei kommen jedes
Mal dieselben unvollständigen Bytes zurück. Wörtlich gelesen wäre die
Ruhephase damit für den Rest der Lage unerreichbar, sobald irgendwo eine Zeile
steht, die niemand mehr vervollständigt — der Fall, den §8.1 ausdrücklich
kennt („der Schreiber ist mitten in einer Zeile endgültig ausgefallen"). Und
ohne Ruhephase gibt es keinen Konvergenzvergleich.

Gemessen wird deshalb, ob das **gesehene Dateiende** vorgerückt ist.
`Pollergebnis` führt dafür `fortschrittBytes` neben `gelesenBytes`. Dieselbe
Unterscheidung trifft `Dateilage.gesehenesEnde` schon für den Verfall aus §6.2.

### 2.2 Zwei aufeinanderfolgende Durchläufe reichen gegen die Caches aus §6.6 nicht

§7.6 verlangt zwei leere Durchläufe je Takt und begründet sie damit, dass „ein
einzelner leerer Takt A auch dann entsteht, wenn ein anderer Client gerade
zwischen zwei Zeilen steht". Das deckt nur den Fall ab, in dem der Leser die
Wahrheit sieht. Der Verzeichnis-Cache hält seine Auskunft 10 Sekunden, der
`FileNotFound`-Cache 5; bei einem Takt B von 4 Sekunden (§6.2) werden **zwei**
aufeinanderfolgende Durchläufe aus derselben zwischengespeicherten Auskunft
bedient. Zwei leere Durchläufe belegen dann nicht, dass nichts da ist.

Die Simulation fordert deshalb `2 + ⌈Cache / Takt⌉` leere Durchläufe. Ohne
Caches sind das genau die zwei aus §7.6.

**Erledigt am 2026-09-09** (Entscheidung 15, dritte Nacharbeit). Beides steht
jetzt in §7.6 selbst; die Simulation weicht an diesen zwei Stellen nicht mehr
vom Konzept ab. `s1 akte pruefe` (Paket V.3) ist noch nicht gebaut und wird
gegen die neue Fassung gebaut.

---

## 3 Ergebnis des Abnahmelaufs

Plan: 4 Clients, 2.000 Kommandos, 5 Phasen, Startwert 20260904, alle
Störungen. Segmentgröße im Lauf 24 KiB statt der 4 MiB aus §10 — sonst käme
kein Segmentwechsel vor, und weder §4.3 noch die Verfallsregel aus §6.2 noch
die aufsteigende Spiegelung aus §5.4.4 würden angefasst. Der Startwert selbst
bleibt unverändert.

| Phase | Kommandos | Ruhe | Ausgang nach §7.6 | lokal ↔ Share |
|---|---|---|---|---|
| 1 | 400 | nach 6 Runden | Konvergenz nachgewiesen, 4 Clients | 12/12 |
| 2 | 800 | nach 6 Runden | Konvergenz nachgewiesen, 4 Clients | 21/21 |
| 3 | 1.200 | nach 6 Runden | Konvergenz nachgewiesen, 4 Clients | 31/31 |
| 4 | 1.600 | nach 7 Runden | Konvergenz nachgewiesen, 4 Clients | 41/41 |
| 5 | 2.000 | nach 8 Runden | **zu wenige Clients ohne Quarantäne** — und die Zustände decken sich **nicht** (siehe Abschnitt 7) | 56/56 |

Der rote Ausgang aus §7.6 — gleiche Versionsvektoren, verschiedener
`zustandsHash` — ist in keiner Phase eingetreten.

**Dieser Lauf ist trotzdem kein sauberer Nachweis.** Abschnitt 7 hält fest,
warum: Die letzte Phase endet damit, dass die Clients verschiedene Zustände
halten, und das Abnahmekriterium wertet das an dieser Stelle nicht. Ob das
Abbruchkriterium von M0 („Nichtkonvergenz unter Störung") erreicht ist, ist
damit **offen** und nicht, wie hier zunächst geschrieben stand, verneint.

Jede vom Plan geforderte Störung ist eingetreten; der Lauf wertet das Ausbleiben
einer geforderten Störung als Mangel und nicht als Warnzeile. Im Lauf oben:
25 Kills, 11 Partitionen, 11 Uhrsprünge, 78 abgeschnittene Share-Anhänge,
52 abgeschnittene lokale Anhänge, 994 Rename-Fehler, 324 blockierende Aufrufe,
dazu Verzeichnis- und `FileNotFound`-Cache, verzögerte Sichtbarkeit,
Schreibrechtentzug, Profilklon und zehn Beschädigungen nach §8.2.

**Warum die Beschädigung nach §8.2 nur in der letzten Phase injiziert wird:**
§8.2 Punkt 5 prüft eine Quarantänestelle bei jedem Programmstart **einmal**
erneut; ein echter Defekt bleibt. Der Weg zurück führt nach §8.6.1 Regel 4
über ein **Ersatzsegment** (§4.6) — die beschädigte Datei selbst wird nie
repariert, denn ein Leser darf keine fremde Datei beschreiben. Ein Leser, der
einmal in Quarantäne gefallen ist, bleibt es. Würde früh beschädigt, stünden
bald alle vier Clients in Quarantäne, jede weitere Phase fiele in den dritten
Ausgang, und der Lauf wäre grün, ohne irgendetwas nachzuweisen.

Die Reihenfolge innerhalb einer Runde ist dabei nicht beliebig: erst spiegeln,
dann stören, dann lesen. Ein Leser liest nach §6.2 ab seinem `leseOffset` und
kommt nie zurück; eine Beschädigung in bereits gelesenen Bytes träfe
ausschließlich den Schreiber, und §8.2 bliebe ungeprüft.

---

## 4 Was §4.6 nicht heilt — der wichtigste Befund dieses Pakets

§8.6.1 Regel 4 sagt zu: „Weil Ausgang B eine Beschädigung ohne fremde
Schreibspur ist, schreibt er den fehlenden Teil als **Ersatzsegment** nach §4.6
neu. Danach gilt die Konvergenzzusage für die betroffenen Leser wieder."

**Diese Zusage hält nicht, wenn die Beschädigung erst entdeckt wird, nachdem
der Schreiber das Segment verlassen hat.**

Der Ablauf, im Lauf beobachtet:

1. Der Schreiber füllt Segment 3, schließt es mit einer Abschlusszeile (§4.3)
   und schreibt in den Segmenten 4, 5, 6 weiter.
2. Segment 3 wird auf dem Share in der Mitte verfälscht. Die Leser fallen dort
   nach §8.2 in Quarantäne — ihr Spiegel von Segment 3 endet an der
   Fehlerstelle.
3. Der Schreiber findet den Schaden beim nächsten Öffnen (§4.6.1 Auslöser 1)
   und schreibt ein Ersatzsegment 7. Es enthält nach §4.6 Schritt 4 die
   Ereignisse **des Segments 3** ab der Fehlerstelle — mehr nicht.
4. Die Leser können das Ersatzsegment lesen: Sein Anker ist die Kette an der
   Fehlerstelle, und genau dort endet ihr Spiegel. Segment 3 ist geheilt.
5. **Die Segmente 4, 5 und 6 bleiben unlesbar.** Ihr Kettenanker ist nach §2.3
   die letzte Zeile ihres jeweiligen Vorgängers; für Segment 4 ist das die
   Abschlusszeile von Segment 3, und die liegt hinter der Quarantänestelle.
   Der Leser kann sie nicht prüfen, ohne unbestätigte Daten als bestätigt zu
   führen — was §8.2 ausdrücklich verbietet.

Die Folge ist genau das, was §8.2 Punkt 7 auch sagt: „Die Konvergenzzusage ist
für diesen Arbeitsplatz ausgesetzt, solange die Quarantäne besteht." Sie steht
allerdings im Widerspruch zu §8.6.1 Regel 4, die die Zusage nach dem
Ersatzsegment wieder gelten lässt. Der Widerspruch fällt erst auf, wenn die
Beschädigung nicht sofort entdeckt wird — und §4.6.1 Auslöser 1 legt fest,
dass sie es im Regelfall erst beim nächsten Öffnen wird.

**Für Johannes.** Das ist eine Entscheidung am Konzept, nicht am Code. Drei
Richtungen sind denkbar, und M0.4 trifft keine davon:

1. §8.6.1 Regel 4 präzisieren: Die Zusage gilt nur für Ereignisse des
   ersetzten Segments; alles danach bleibt für den betroffenen Leser
   ausgesetzt, bis er den Einsatz mit einem gesunden Spiegel neu aufsetzt
   (§8.6.1, Exportweg).
2. Das Ersatzsegment auf **alle** Segmente ab der Fehlerstelle ausdehnen. Das
   ist teuer und verändert §4.6 erheblich.
3. Dem Leser erlauben, ein Folgesegment ohne prüfbaren Anker zu lesen und als
   „ungeprüfte Kette" zu kennzeichnen. Das bricht mit §8.2 und mit §1.3.

Bis zur Entscheidung wertet die Simulation diesen Ausgang als „nicht
vergleichbar" nach §7.6 — kein Fehler, aber auch kein Nachweis — und meldet die
Quarantänen getrennt, wie §8.6.1 Regel 3 es verlangt.

---

## 4a Was die Simulation in `@s1/speicher` gefunden hat

Zwanzig Befunde, alle in M0.3 entstanden und von den Unit-Tests dort nicht
gefunden. Der gemeinsame Grund: Die Prüfhilfe von M0.3 lässt einen Aufruf
**vor** seiner Wirkung scheitern. Ein `write`, der ein Präfix schreibt und
danach scheitert — §5.4.1 nennt genau diesen Fall für den Share —, kam dort
nicht vor.

| # | Befund | Wirkung im Feld |
|---|---|---|
| 1 | `schreiber.json` und `upload-state.json` warfen bei einem Dateisystemfehler | Ein Rename-Fehler (§6.4) riss den ganzen Bedienschritt beziehungsweise Poll-Durchlauf ab — obwohl §9 verlangt, dass er folgenlos bleibt |
| 2 | Ein teilweise gelungener lokaler Anhang blieb liegen | Die eigene Datei war ab dieser Stelle unauswertbar; die Spiegelung kam nicht mehr vorbei, jeder Leser fiel dort in Quarantäne — und der Arbeitsplatz schrieb weiter, ohne dass etwas ankam |
| 3 | Scheiterte der Anhang an den lokalen Spiegel, flog der Fehler aus dem Poll | §8, Grundsatz: „Kein Fehlerbild führt zum Stillstand des Lesers" — alle anderen Dateien standen mit still |
| 4 | Nach einem solchen Fehler wurde beim nächsten Mal **hinter** das Bruchstück angehängt | Der Spiegel war kein Präfix der Share-Datei mehr; §8.6.1 Regel 4 („Der ausgeleitete Spiegel enthält nur geprüfte Zeilen") galt nicht mehr |
| 5 | `ENOENT` wurde mit „die Datei ist leer" gleichgesetzt | Unter dem `FileNotFound`-Cache (§6.6) doppelte Ereigniszeilen in der eigenen Datei — der eine Fehler, den §5.4.2 ausschließen soll |
| 6 | Der erste Schutz dagegen lebte nur in der Sitzung | Ein Neustart innerhalb der fünf Sekunden erzeugte denselben Schaden; entschieden wird jetzt mit `wx` auf dem Server |
| 7 | Der Kettenanker nahm die *bisher* letzte Zeile des Vorgängersegments | Ein Leser, der den Vorgänger halb gespiegelt hatte, setzte eine **gesunde** Datei dauerhaft in Quarantäne (§8.2) — mit einer nachweislich falschen Aussage über einen anderen Arbeitsplatz |
| 8 | Ruhephase an `gelesenBytes` statt am Fortschritt | Eine unvollständige Zeile, die niemand mehr vervollständigt (§8.1), machte die Ruhephase für den Rest der Lage unerreichbar — und damit den Konvergenzvergleich |
| 9 | Der Fortschritt wurde beim Lesen gebucht, nicht bei der Übernahme | Der Takt, in dem die Ereignisse in den Fold liefen, galt als Ruhetakt |
| 10 | §4.5 Schritt 6 kürzte auf den *gemerkten* `shareOffset` | Nach einem abgebrochenen Übertragungsversuch verschwanden lokal Zeilen, die auf dem Share stehen |
| 11 | Dieselbe Kürzung riss bei einem lokalen Dateisystemfehler den Spiegelungslauf ab | §8.8 verlangt eine sichtbare Abweisung, nicht den Abbruch |
| 12 | Ein nie gespiegeltes Segment blieb als 0-Byte-Datei zurück | Der Versionsvektor dieses Clients war dauerhaft ein anderer; der Vergleich nach §7.6 fiel für den Rest der Lage in den dritten Ausgang |
| 13 | Die Kürzung ging unter `shareOffset` | **Datenverlust**: lokal gelöschte Zeilen, die §4.5 Schritt 3 nicht mitnimmt und die auf dem Share ab der Fehlerstelle in Quarantäne stehen |
| 14 | Der Reparaturmerker des Schreibers lag am Schreiber, nicht an der Datei | Ein Ersatzsegment quittierte ihn auf der falschen Datei; das Bruchstück blieb, und der Start brach von da an **immer** ab — der Client kam an seine eigene Akte nie wieder heran |
| 15 | Der Start brach bei einem Bruchstück in einem nicht letzten Segment ab | Dasselbe Bild, anderer Weg dorthin |
| 16 | Ein halb vollzogener Ersatzsegment-Wechsel blieb stehen | Die nächste Zeile ging ohne `SegmentErsetzt` davor hinaus; der nächste Start brach ab |
| 17 | Der Offset in der Nutzlast des Ersatzsegments war eine Share-, keine lokale Zeilengrenze | `kettenanker` fand ihn nicht; der nächste Start brach ab |
| 18 | Beim Öffnen wurde nur **ein** beschädigtes eigenes Segment repariert | §4.6.1 Auslöser 1 spricht vom Plural; die übrigen Quarantänen blieben bis zum nächsten Start bestehen |
| 19 | Der Laufnummernvergleich zog die aufgegebenen Kennungen mit ein | Dieselbe Klon-Zeile löste bei **jedem** Öffnen einen Kennungswechsel aus; der Bediener las jedes Mal, sein Profil sei kopiert worden |
| 20 | `#kuerzeSpiegel` verließ sich auf die Aufrufreihenfolge | Latent: `kuerzeAuf` verlängert mit Nullbytes, wenn die Länge über der Datei liegt. Im Produktivpfad nicht auslösbar, über die öffentliche Schnittstelle des Lesers schon |

Sechs davon — 2, 5, 13, 14, 16 und 17 — hätten im Feld dauerhaften Schaden
angerichtet: verlorene Einträge, oder einen Arbeitsplatz, der sich nicht mehr
öffnen lässt.

---

## 4b Zahlen für M0.5 — §10

Gemessen in einem Lauf mit allen Störungen **außer** der Beschädigung nach
§8.2. Mit ihr liest ein neu hinzukommender Arbeitsplatz weniger — er erbt
dieselbe Quarantäne wie alle anderen (Abschnitt 4) —, und die Zahl verstünde
sich falsch als Kostenangabe.

| Annahme | Gemessen | Was noch offen ist |
|---|---|---|
| **A2** — 400 bis 600 Byte je Ereignis (§2.6) | **581 Byte im Mittel** über 2.000 Ereignisse, Spanne **467 bis 747** | Nichts. Die Zeilenlänge hängt am Zeilenformat aus §2.1 und an den Nutzlasten, nicht am Share. Die Annahme **trägt im Mittel**, liegt aber am oberen Rand, und die Spanne reicht über 600 hinaus — für die Abschätzung der Dateigrößen nach §4.2 ist mit 600 zu rechnen, nicht mit 500 |
| **A7** — Erstlauf ohne fremde Schnappschüsse bleibt bezahlbar (§7.5) | **1,18 MB in 2.045 Zeilen aus 49 Dateien**, in 3 Takt-Durchläufen vollständig | Die **Zeit** dafür. Sie ist das Produkt aus dieser Datenmenge und der SMB-Lesegeschwindigkeit, und die misst M0.5 |
| **A10** — Vollprüfung der eigenen Share-Segmente beim Öffnen (§4.6.1 Auslöser 1) | **181 kB je Client**, **15 %** dessen, was der Erstlauf ohnehin liest | Ebenfalls die Zeit. Der Anteil bestätigt die Begründung in §4.6.1 („die eigenen Segmente sind ein Fünftel davon") |

Zur Einordnung von A7: 49 Dateien nach 2.000 Ereignissen sind mehr, als §6.2
für den Normalbetrieb ansetzt — der Lauf erzeugt durch den Segmentwechsel bei
24 KiB und durch Kennungswechsel viele Dateien. Für die Kostenrechnung von
M0.5 ist die **Datenmenge** maßgeblich, nicht die Dateizahl; für die
Poll-Kosten die Zahl der Dateien in Takt A, und die begrenzt der Verfall
(§6.2).

Eine Zahl liefert dieser Lauf **nicht**: Annahme **A5** (§10) — ob eine
Neuöffnung auf dem Synology-Gerät zum Server durchgeht. Die feindliche Schicht
setzt sie voraus (§6.6, Abschnitt 1.1) und kann sie deshalb nicht prüfen. Sie
gehört zu M0.5.

---

## 5 Reproduzierbarkeit

Der Lauf hängt allein am Startwert: Zwei Läufe mit demselben Plan ergeben
dieselben `zustandsHash`-Werte, dieselben Ereigniszahlen und denselben
Berichtstext. Es gibt kein `Math.random` und kein `Date.now` in der
Simulation; alle Zeiten kommen aus der virtuellen Uhr, jede Störquelle hat
einen eigenen Abzweig des Zufallsstroms, und die einzige
Verzeichnisauflistung, die in den Bericht eingeht, wird sortiert.

Ein Sweep über achtzehn Startwerte mit dem vollen Abnahmeplan war zum
Zeitpunkt dieser Fassung **nicht abgeschlossen**: acht Startwerte waren geprüft
und bestanden, ein unabhängiger Gutachter fand unter acht anderen Startwerten
**zwei rote Läufe**. Siehe Abschnitt 7.

---

## 6 Was dieser Lauf **nicht** zeigt

* **Keine Zeit.** Die virtuelle Uhr macht Fristen prüfbar und Latenzen
  bedeutungslos. Jede Aussage über Sekunden gehört in M0.5.
* **Kein echtes SMB.** Die feindliche Schicht bildet die Caches aus §6.6 mit
  den dort genannten Werten nach; ob das Synology-Gerät sich so verhält, ist
  Annahme A5 und wird gemessen.
* **Keine Schnappschüsse.** §7 ist nicht gebaut (M0.3 hat sie ausgelassen).
  Die Eigenschaft aus §7.1 — „Wird jeder Schnappschuss gelöscht, muss sich
  derselbe Zustand allein aus den Ereignisdateien ergeben" — ist deshalb im
  heutigen Stand **leer**: Es gibt nichts zu löschen. Der Konvergenzvergleich
  faltet ohnehin ausschließlich aus den Ereignisdateien; die Eigenschaft ist
  damit trivial erfüllt und **nicht geprüft**. Sie ist mit §7 nachzuholen.
* **Kein Electron, kein Worker, kein IPC.** Das ist M2.1. Die Simulation läuft
  einsträngig; die Reihung aus §8.4 („kein zweiter Versuch, solange der erste
  unterwegs ist") ist deshalb nur dort geprüft, wo die Speicherschicht sie
  selbst herstellt, nicht gegen echte Nebenläufigkeit.
* **Nur ein Betriebssystem.** Dieser Lauf ist auf macOS entstanden. Auflage 17
  und M0.6 verlangen Windows und Linux; die CI-Jobs sind dafür da.
* **Kein Nachweis über die Pfadbindung des Reparaturmerkers.** Sie ist durch
  keine einzelne Mutation des Produktionscodes zu töten, weil die Kürzung beim
  Start denselben Schaden abfängt. Geprüft ist die Wirkung, nicht jeder ihrer
  beiden Träger einzeln.

---

## 7 Offen geblieben — Befunde des zweiten Gutachtens

Ein zweiter, unabhängiger Gutachterdurchgang gegen den Stand `0ab8891` hat drei
Blocker gefunden. **7.1 und 7.3 sind behoben, 7.2 nur teilweise.** Der
Abnahmelauf ist damit **rot** — und das ist die eigentliche Nachricht dieses
Abschnitts: Der grüne Lauf aus Abschnitt 3 war ein Artefakt des schwachen
Kriteriums, das 7.3 beschreibt.

### 7.1 Ein zweites Ersatzsegment verliert alle Ereignisse — **behoben**

`schreiber.ts`, `#liesEigenesSegment` zieht die Kette über die eigenen Segmente
von Hand durch, statt sie je Segment mit `kettenanker` zu bestimmen. Ein
Ersatzsegment setzt nach §2.3 aber nicht auf dem Ende seines Vorgängers auf,
sondern auf der letzten unbeschädigten Zeile des ersetzten Segments. Wird ein
**Ersatzsegment** ein zweites Mal beschädigt, liest der Schreiber es als
kettenfalsch, findet keine Zeile und schreibt ein Ersatzsegment, das nur seine
eigene `SegmentErsetzt`-Zeile enthält — mit `abOffset: 0`. §4.6 Schritt 4
(„schreibt alle Ereignisse ab dieser Stelle noch einmal") ist damit verletzt,
und der Bediener liest trotzdem „er wird neu geschrieben".

`schreiberStart.ts` macht es richtig und warnt im Kommentar wörtlich vor genau
diesem Fehler. Nachgestellt und bestätigt.

**Behoben:** Der Anker kommt jetzt aus `kettenanker`; ist er nicht bestimmbar,
wird die Reparatur sichtbar abgewiesen statt ins Blaue geschrieben. Mit einem
Regressionstest belegt, der bei der Rückkehr zur alten Fassung fällt.

### 7.2 Ereignisse, die auf keiner Share-Datei stehen — **teilweise behoben, weiter offen**

Startwert 6, deterministisch reproduzierbar: 2.050 Identitäten lokal beim
Schreiber, 2.025 auf dem Share. Der Lauf **erkennt** das jetzt selbst und
benennt jede verlorene Identität (siehe 7.3).

**Eine Ursache ist gefunden und behoben:** Bricht das Wiederholen der
Ereignisse an einer lokalen Schreibstörung ab (§8.8), steht die
`SegmentErsetzt`-Zeile bereits, und `ersetzteSegmente` nahm das Vorbild damit
dauerhaft aus der Prüfung — obwohl der größere Teil seiner Ereignisse nirgends
wiederholt war. Gezählt wird jetzt nur ein **vollständiges** Ersatzsegment.

**Zwei weitere Ursachen gefunden, eine davon im Messaufbau.**

*Ein Messfehler der Ruhephase.* §4.6.1 Auslöser 1 hängt am **Programmstart**.
Bricht das Wiederholen der Ereignisse an einer lokalen Schreibstörung ab
(§8.8), wird der Rest beim **nächsten** Öffnen nachgeholt — das ist nach §4.6
richtig. Die Ruhephase öffnete aber genau einmal, maß damit einen
Zwischenstand und nannte ihn Verlust. Sie öffnet jetzt so oft, bis nichts mehr
zu reparieren ist, und meldet es als Mangel, wenn das nach fünf Öffnungen noch
aussteht.

*Ein echter Fehler, den erst diese Wiederholung sichtbar gemacht hat.*
`ketteAnStelle` nahm für Offset 0 die Kette am Ende des **Vorgängersegments**.
Für ein gewöhnliches Segment stimmt das (§2.3); für ein **Ersatzsegment**
nicht — es setzt nach §4.6 Schritt 3 mitten im ersetzten Segment auf. Wird ein
Ersatzsegment selbst gleich an seiner ersten Zeile beschädigt, ersetzt der
Ersatz des Ersatzes es ab Offset 0, bekommt einen Anker, den
`bereiteSchreiberVor` nicht nachvollziehen kann — und der Client kommt an
seine eigene Akte nie wieder heran (§8, Grundsatz; §8.8 Punkt 5). Im Lauf
konkret beobachtet: `00000004.0015` ersetzt `0013` ab Offset 0, und `0013` war
selbst ein Ersatzsegment für `0002`. Das erklärt rückwirkend auch die
`LokalerKettenbruch`-Abbrüche, die während dieses Pakets dreimal aufgetreten
und jeweils anders erklärt worden waren.

*Und eine dritte, die den Rest erklärt: jeder Reparaturanlauf fing von vorn an.*
Nach einem Abbruch legte die Vollprüfung ein **neues** Ersatzsegment an, statt
das angefangene fortzusetzen. Damit musste **ein** Anlauf alle Zeilen am Stück
schaffen — bei 33 zu wiederholenden Zeilen und einer Störwahrscheinlichkeit je
Anhang ist das Glücksspiel. Im Lauf standen am Ende fünf unvollständige
Ersatzsegmente für dasselbe Vorbild da (8, 22, 30, 22 und 5 von 33 Zeilen), und
die letzten vier Ereignisse erreichte kein Leser mehr. Ist das laufende Segment
bereits ein Ersatzsegment für dasselbe Vorbild, werden jetzt genau die noch
fehlenden Zeilen angehängt; jeder Anlauf ist ein Fortschritt. §4.6 steht dem
nicht entgegen — die Datei ist append-only, die Kette läuft weiter, und die
wiederholten Zeilen behalten ihre Identität.

**Geschlossen.** Fünf geprüfte Startwerte (20260904, 1, 6, 7, 999) mit dem
vollen Abnahmeplan, 6 bis 10 injizierten Beschädigungen je Lauf: **kein
einziges verlorenes Ereignis**. Vorher: 26, 32, 21, ein Abbruch und ein roter
Lauf.

**Offen bleibt** der Sweep über achtzehn Startwerte: Der letzte vollständige
lief gegen ein Kriterium, das nach 7.3 noch zu schwach war. Er ist mit dem
jetzigen Kriterium zu wiederholen — rund zwanzig Minuten Rechenzeit, kein
Denkaufwand.

### 7.3 Das Abnahmekriterium bewertet den schwereren Fall milder — **behoben**

`lauf.ts`, `bewerte`: Der Ausgang `zuWenigeClients` wird in der letzten Phase
gar nicht bewertet, während der Nachbarzweig `nichtVergleichbar` genau die
analoge Größe (`gleicheHashes`) auswertet. Sachlich derselbe Zustand — die
Clients halten verschiedene Zustände — ist damit einmal ein Mangel und einmal
nicht, und **je mehr Clients beschädigt sind, desto eher besteht der Lauf**.
Der Referenzlauf oben ist grün, weil diese Ausnahme greift; sein eigener
Bericht sagt daneben „alle Zustände decken sich trotzdem: nein". Gegen
Auflage 18 („zählbares Abbruchkriterium") ist das nicht tragfähig.

Die Begründung der Ausnahme — §8.2 Punkt 7 setzt die Konvergenzzusage aus,
solange die Quarantäne besteht — trägt für die Konzeptlücke aus Abschnitt 4.
Sie trägt **nicht** für einen Verlust, der nichts mit §4.6 zu tun hat (7.2).

**Behoben, und zwar durch Trennung.** Beide Ausgänge werden jetzt gleich
behandelt: berichtet, nicht bewertet — wie §7.6 („kein Fehler, aber auch kein
Nachweis") und §8.2 Punkt 7 es vorgeben. An ihre Stelle tritt die Prüfung, auf
die es sachlich ankommt: **jedes Ereignis, das ein Client geschrieben hat, muss
auf dem Share auswertbar sein.** Sie trifft den Verlust direkt, statt ihn aus
einem Hash-Unterschied zu erraten, und sie unterscheidet ihn von dem, was §8.2
Punkt 7 zulässt — eine Quarantäne nimmt einem **Leser** den Zugang zu Bytes,
die vorhanden sind; ein Verlust heißt, dass sie nirgends mehr stehen. Der
Bericht nennt jede verlorene Identität mit Client.

Genau diese Prüfung macht den Abnahmelauf rot. Sie ist damit das erste
Kriterium dieses Pakets, das etwas entscheidet.

### 7.4 Weitere offene Punkte — **abgearbeitet am 2026-09-09**

* `akte.ts`: Das Nachholen der Kürzung beim Öffnen (`uebernahmeGelungen =
  false`) setzte dieselbe Gewissheit voraus, die sein eigener Kommentar
  verneint. **Ausgelöst und behoben.** Der Weg ist nachgestellt: Bricht der
  Kennungswechsel beim Übernehmen an einer lokalen Schreibstörung ab (§8.8),
  stehen die betroffenen Zeilen auf keiner Share-Datei und unter der neuen
  Kennung auch nicht — und die Kürzung beim nächsten Öffnen schnitt sie weg.
  Gekürzt wird jetzt erst, nachdem §4.5 Schritt 3 für genau diese Zeilen
  nachgeholt ist; welche das sind, entscheidet die Identität. Scheitert das
  Nachholen, bleibt die Datei unangetastet und der Versuch wird beim nächsten
  Öffnen wiederholt. Zwei Tests, beide gegen die Mutation „Absicherung
  entfernt" geprüft.
* Vier Korrekturen ohne Testdeckung — die Pfadbindung des Reparaturmerkers,
  der Parameter `quelleIstVollstaendig`, die Längenprüfung in `#kuerzeSpiegel`
  und `abGrenze` in der Nutzlast des Ersatzsegments. **Alle vier sind
  festgenagelt**, jeder Test seinerseits mit einer Mutation des
  Produktionscodes geprüft. **Nicht** eingelöst ist der Endschaden zu
  Befund 14: Ein Test, der zeigt, dass das Bruchstück für immer stehenbleibt,
  gibt es weiterhin nicht — innerhalb einer Sitzung kehrt der Schreiber nie
  zur alten Datei zurück, und über einen Neustart fängt `bereiteSchreiberVor`
  die Kürzung ab. Festgenagelt ist die andere Richtung derselben Bindung: Der
  Merker darf nicht auf eine fremde Datei greifen.
* `klient.ts` protokolliert `weitereReaktionen` jetzt. **Behoben** — der
  Bericht zeigt damit, ob mehrere Reparaturen je Öffnen gelungen sind (§4.6.1
  Auslöser 1 im Plural).
* `oeffnungspruefung.ts`: `hoechsteLaufnummerAufShare` ist **entfernt**. Mit
  dem Feld entfällt je Öffnen ein vollständiger Lesedurchgang über jede Datei
  jeder aufgegebenen Kennung.
* Das `console.log` in `stoerfestigkeit.test.ts` ist weg.

### 7.5 Sweep über achtzehn Startwerte, wiederholt — **sechs Läufe fallen**

Der Sweep aus 7.2 lief gegen ein Kriterium, das nachweislich zu schwach war
(7.3). Er ist am 2026-09-09 gegen das nachgeschärfte Kriterium wiederholt
worden: `abnahmePlan()` mit überschriebenem `startwert`, achtzehn Werte,
frischer Ordner je Lauf, sechs Läufe gleichzeitig, rund dreieinhalb Minuten je
Lauf.

| | |
|---|---|
| Läufe | 18 |
| bestanden | 12 |
| gefallen | 6 — Startwerte 3, 5, 101, 111, 999, 12345 |
| roter Ausgang (`abweichend`) | **0 in allen 18 Läufen** |
| Läufe mit verlorenen Ereignissen | 5 (3, 5, 101, 111, 12345) |
| Läufe ohne Konvergenznachweis vor der letzten Phase | 1 (999, zwei Phasen) |

**Die Läufe sind reproduzierbar.** Startwerte 3 und 5 wurden je zweimal
wiederholt, gleicher Bau, frische Ordner, vier Läufe gleichzeitig: identische
Verlustlisten bis auf die einzelne Ereignis-Identität. Der gegenteilige Befund
aus dem dritten Gutachterdurchgang („derselbe Startwert liefert verschiedene
Läufe") erklärt sich daraus, dass während jenes Durchgangs `dist` neu gebaut
wurde — die beiden verglichenen Läufe liefen gegen verschiedene Stände.

**Die Verluste sind älter als die Korrektur aus Commit `ed2a204`.** Vier
Startwerte wurden zusätzlich gegen den Stand *vor* dieser Korrektur gefahren:

| Startwert | vorher | nachher |
|---|---|---|
| 5 | 1 Verlust, dieselbe Identität | 1 |
| 111 | 6 Verluste, dieselben Identitäten | 6 |
| 101 | 2 Verluste, dieselben Identitäten | 2 |
| 3 | **0** | 7 |

Drei von vier reproduzieren exakt. Startwert 3 ist der Vorbehalt: Dort ändert
die Korrektur den Ablauf und der Lauf fällt danach. Das ist kein Beleg für
einen Rückschritt — jede Verhaltensänderung verschiebt den gesamten weiteren
Ablauf, und die Verluste bei 3 tragen dieselbe Signatur wie die der übrigen —
aber es ist auch nicht ausgeräumt und steht hier als offener Punkt.

**Die Ursache ist gefunden und liegt in §4.6.** An Startwert 111 Zeile für
Zeile nachgemessen: Das Ersatzsegment übernimmt den Inhalt ab dem
*aufgezeichneten Übertragungsstand*, während die Share-Datei nur bis zur
*Beschädigungsstelle* auswertbar ist. Die Zeilen dazwischen — im Lauf `:397`
bis `:400` und `:434` folgende — stehen auf dem Share, sind hinter der
Beschädigung unerreichbar und werden vom Ersatz nicht mitgenommen. Sie sind für
jeden Leser fort. Die Ruhephase bemerkt es nicht, weil §7.6 Bedingung 1
ersetzte Segmente ausnimmt. Der vollständige Beleg und die drei möglichen
Richtungen stehen als **Entscheidung 16** in `../04-OFFENE-ENTSCHEIDUNGEN.md`;
sie ist bewusst nicht getroffen, weil sie §4.6 ändert.

Ein Teil der Verluste — Startwerte 5 und 12345, ein Ereignis in Lauf 3 — trägt
das Präfix einer **aufgegebenen** Kennung. Diese Gruppe deckt sich mit dem
Blocker aus dem dritten Gutachterdurchgang: `pruefeBeimOeffnen` bildet seine
Vergleichsmenge allein aus dem laufenden Präfix, die Dateien aufgegebener
Kennungen werden nur noch für die Laufnummer gelesen. Eine Beschädigung dort
wird von niemandem mehr entdeckt. Auch das gehört zu Entscheidung 16
beziehungsweise 14 und ist nicht im Code entschieden worden.

**Startwert 999** fällt aus einem anderen Grund: zwei Phasen ohne
Konvergenznachweis, ohne Verlust. Nicht weiter untersucht.

### 7.6 Weitere Befunde des dritten Gutachterdurchgangs — **alle sechs abgearbeitet am 2026-09-09**

Der dritte Durchgang bekam die zuletzt geänderten Dateien. Seine beiden
Blocker sind oben abgehandelt: der erste ist widerlegt (7.5, „Die Läufe sind
reproduzierbar"), der zweite ist als Entscheidung 16 aufbereitet. Die
folgenden sechs Befunde sind in der zweiten Nacharbeit abgearbeitet worden;
was jeweils geschah, steht unter dem Befund. Die Kennzeichnung ist seine
eigene: *beobachtet* heißt ausgelöst, *hergeleitet* heißt am Code
argumentiert.

* **`darfLoeschen` gilt für Kennungen, die die Übernahme nie berührt hat**
  (`akte.ts:400–412` gegen `:539–547`). *Hergeleitet.* `#wechsleKennung`
  übergibt alle früheren Kennungen mit `darfLoeschen = true` und begründet das
  damit, dass ihr Inhalt „nachweislich anderswo steht". Gesammelt werden aber
  nur Zeilen des **gerade** aufgegebenen Präfixes; für ältere Kennungen gilt
  die Gewissheit nicht. Voraussetzung: ein vorangegangener Kennungswechsel,
  dessen Übernahme nach §8.8 abgebrochen ist.

  **Behoben** (`7b39fea`). Die Gewissheit gilt jetzt je Präfix: Der Parameter
  nennt das eine Präfix, für das die Übernahme gerade gelungen ist. Test mit
  zwei aufeinanderfolgenden Kennungswechseln, der erste mit abgebrochener
  Übernahme.
* **Nach einem abgebrochenen Kennungswechsel wird der Klon bis zum Neustart
  nicht mehr gelesen** (`akte.ts:400–412`, `:418–420`). *Hergeleitet.*
  `gleicheMitSpiegelAb` setzt den Leseoffset der aufgegebenen Datei auf die
  volle lokale Länge, also hinter das Share-Ende. Das Öffnen holt es nach,
  innerhalb der Sitzung bleibt der Klon unsichtbar.

  **Behoben** (`7b39fea`). Der Spiegelabgleich nimmt eine Obergrenze entgegen;
  solange die Kürzung aussteht, ist das das gemeinsame Präfix von lokalem
  Spiegel und Share-Datei an einer Zeilengrenze. Zusätzlich wird die Kürzung
  nach einem gescheiterten Anlauf sofort mit der Nachhol-Bedingung wiederholt.
* **Das Abnahmekriterium misst gegen die überlebenden lokalen Dateien**
  (`lauf.ts:439–473`). *Hergeleitet.* Die Sollmenge stammt aus der
  Verzeichnisauflistung. Eine lokal **gelöschte** Zeile fehlt damit auf beiden
  Seiten und fällt nicht auf — gerade der Schaden aus Befund 13. Die Ist-Menge
  läge bereit: `klient.ts:301` sieht jede erfolgreich geschriebene
  Ereignis-Identität, merkt sich aber nur den Zähler.

  **Behoben** (`af31ce0`). `Klient.geschriebeneIdentitaeten` führt die
  quittierten Identitäten; sie gehen als zweite Quelle in die Sollmenge ein
  und überleben jedes Löschen und jeden Kennungswechsel.
* **Vier der geforderten Störungen werden nicht erzwungen**
  (`bericht.ts:23–44`). *Beobachtet.* `WEITERE_STOERUNGEN` nimmt
  `beschaedigung`, `profilKlon`, `schreibrechtEntzug` und
  `lokaleSchreibstoerung` von der Mangelprüfung aus. Ein Lauf kann grün sein,
  ohne §8.2 und §4.6 auch nur berührt zu haben. Abschnitt 3 dieses Protokolls
  behauptet das Gegenteil und ist an dieser Stelle **falsch**.

  **Behoben** (`d75c4f2`). Die Liste `WEITERE_STOERUNGEN` ist entfallen; alle
  vier stehen in `GEFORDERTE_STOERUNGEN`, und `istGefordert` misst die
  Gelegenheiten der Beschädigung an **einer** Phase, weil sie nur in der
  letzten gezogen wird. Abschnitt 3 stimmt damit wieder.
* **Vorläufige Quarantäne (§8.1) zählt wie endgültige (§8.2)**
  (`klient.ts:163–169`, `konvergenz.ts:235–236`). *Beobachtet.* Damit werden
  Phasen unbewertbar aus einem Zustand heraus, den §8.1 ausdrücklich als „kein
  Fehler" führt. Das ist der wahrscheinlichste Grund für Startwert 999.

  **Behoben** (`6998eaf`). `teileQuarantaenen` trennt die Herkunft;
  `vergleiche` wertet nur die endgültigen, der Bericht nennt die vorläufigen
  getrennt. Ob damit Startwert 999 erklärt ist, steht in 7.7.
* **Zwei Kommentare stimmen nicht mit dem Code überein.** *Beobachtet.* Der
  Doc-Kommentar von `ketteAnStelle` (`kettenanker.ts:84–87`) beschreibt für
  `offset === 0` das Gegenteil dessen, was der Code tut — es ist der Text von
  vor der Korrektur zu 7.2. Und `akte.ts:120–127` behauptet, die Schranke sei
  die Zahl der eigenen Segmente; sie ist die feste Zahl 64.

  **Behoben** (`f275d58`). Beide Kommentare sagen jetzt, was der Code tut;
  beim zweiten steht zusätzlich, was beim Erreichen der Schranke geschieht.

### 7.7 Sweep nach der zweiten Nacharbeit — **13 von 18 bestanden, kein Verlust außer einem benannten**

Gefahren am 2026-09-09 gegen den Stand `756f0fe`, achtzehn Startwerte, voller
Abnahmeplan (4 Clients, 2.000 Kommandos, 5 Phasen), frischer Ordner je Lauf,
sechs Läufe gleichzeitig, `dist` **während** des Sweeps unverändert. Die
Startwerte: 1, 2, 3, 4, 5, 6, 7, 42, 99, 101, 111, 999, 4242, 12345, 65536,
20260904, 20260909, 123456789.

| | |
|---|---|
| Läufe | 18 |
| bestanden | 13 |
| gefallen | 5 — Startwerte 2, 6, 999, 12345, 20260909 |
| roter Ausgang (`abweichend`) | **0 in allen 18 Läufen** |
| Läufe mit verlorenen Ereignissen | **1** (12345, zwei Ereignisse) — vorher 5 |

Zum Vergleich der Stand vor der Nacharbeit (7.5): 12 bestanden, 6 gefallen,
fünf davon mit Verlusten. Das Kriterium ist seither an zwei Stellen
**schärfer** geworden (§9 fordert vier weitere Störungen, die Sollmenge
überlebt das Löschen), der Lauf fällt trotzdem seltener.

**Startwert 111 ist behoben.** Er verlor sechs Ereignisse (`:397`–`:400`,
`:434`, `:435`) und besteht jetzt. Ebenso 3, 5 und 101 — alle vier fielen in
7.5 mit Verlusten.

**Startwert 3 ist damit aufgeklärt.** Der offene Punkt aus 7.5 („verliert ohne
die Korrektur aus `ed2a204` nichts und mit ihr sieben") hat sich erledigt: Der
Lauf besteht, ohne ein Ereignis zu verlieren. Ein Rückschritt lag nicht vor;
die sieben Verluste trugen dieselbe Signatur wie die übrigen und sind mit
derselben Korrektur an §4.6 verschwunden.

#### Die fünf gefallenen Läufe, jeder mit benanntem Grund

**2 und 6 und 20260909 — eine geforderte Störung ist nicht eingetreten.**
Startwert 2: `schreibrechtEntzug`. Startwerte 6 und 20260909: `profilKlon`.
Kein inhaltlicher Befund, sondern **ein Würfel**: `profilKlon` steht in
`ALLE_FEHLER` auf 0,001 je Kommando, bei 2.000 Kommandos also ein
Erwartungswert von 2 und damit rund 13 % Wahrscheinlichkeit, gar nicht
einzutreten. Beobachtet sind 2 von 18 Läufen — das deckt sich. Der
Kommentar an `ALLE_FEHLER` („so gewählt, dass jede Störung in 2.000
Kommandos mehrfach vorkommt") trifft für diese beiden nicht zu, und
`istGefordert` verlangt nur einen Erwartungswert von 1, was für sich
genommen 37 % Ausfallwahrscheinlichkeit zulässt. **Nicht geändert**, weil
jede Änderung an den Wahrscheinlichkeiten den ganzen Sweep neu bewertet;
siehe „Was offen bleibt".

**999 — die Versionsvektoren laufen nach einer Reparatur dauerhaft
auseinander.** Zwei Phasen ohne Konvergenznachweis, kein Verlust. Nachgemessen
mit festem Ordner:

* Die Share-Datei `00000003.0005.jsonl` ist 11.085 Byte groß und nach einer
  Beschädigung nur bis Offset **10.404** auswertbar.
* Die Arbeitsplätze 1, 2 und 4 führen ihren Spiegel dieser Datei deshalb bis
  10.404 — genau das, was §5.5 zusagt: „Ab einer Quarantänestelle ist der
  lokale Spiegel einer fremden Datei nicht byteweise identisch mit der
  Share-Datei — er ist ihr geprüftes Präfix."
* Arbeitsplatz 3 ist der Schreiber und hat sie lokal vollständig: 11.860 Byte.

Der Vergleich nach §7.6 stellt daraufhin verschiedene Versionsvektoren fest
und meldet „nicht vergleichbar". **Die Ereignismenge und der Zustand sind in
allen drei Phasen identisch** — der Bericht sagt das ausdrücklich („gleiche
Ereignismenge: ja, gleicher Zustand: ja"). Die Reparatur nach §4.6 hat also
getan, was sie soll; jedes Ereignis ist bei jedem Leser angekommen. Nur der
Vektor kann sich nach einer solchen Reparatur nie wieder decken. Das ist eine
Frage an §7.6, keine am Code — als **Entscheidung 18** aufbereitet.

**Die Vermutung aus 7.6 ist damit widerlegt.** Dort stand, die Gleichbehandlung
von vorläufiger und endgültiger Quarantäne sei „der wahrscheinlichste Grund
für Startwert 999". Sie ist getrennt worden (`6998eaf`), und 999 fällt
weiterhin — aus dem eben genannten, anderen Grund. Geprüft statt angenommen.

**12345 — die Datei einer aufgegebenen Kennung wird von niemandem mehr
repariert.** Zwei Ereignisse verloren, beide unter dem Präfix `65654370`,
geschrieben von einem Arbeitsplatz, der inzwischen unter `0cd9146e` läuft.
Nachgemessen:

* `65654370.0000.jsonl`: 25.399 Byte auf dem Share, auswertbar bis **24.306**.
  Bei genau 24.306 steht lokal `656543703fff2917:377`.
* `65654370.0002.jsonl`: 25.267 Byte, auswertbar bis **24.194**. Bei genau
  24.194 steht lokal `656543703fff2917:468`.

Beide Beschädigungen liegen in Dateien, die der Client nach §4.5 Schritt 6
nicht mehr als eigene führt — „der Spiegel einer fremden Datei, nämlich der
des Klons". `pruefeBeimOeffnen` bildet seine Vergleichsmenge deshalb allein
aus dem **laufenden** Präfix, und niemand entdeckt die Beschädigung. Der
Inhalt liegt beim ursprünglichen Schreiber lokal vollständig vor; §4.5
Schritt 6 verbietet ihm aber, dort noch etwas zu schreiben. Das ist genau der
Fall, den 7.5 unter „ein Teil der Verluste trägt das Präfix einer
aufgegebenen Kennung" beschrieben hat. Er ist **nicht im Code entschieden
worden** — als **Entscheidung 17** aufbereitet.

#### Was offen bleibt

* **Entscheidung 15**, **17** und **18** sind am 2026-09-09 entschieden und in
  der dritten Nacharbeit umgesetzt; siehe Abschnitt 7.8.
* **Die Wahrscheinlichkeiten in `ALLE_FEHLER`.** Solange `profilKlon` bei
  0,001 steht, fällt rund jeder siebte Lauf allein daran. Das ist mit
  Auflage 18 („in einem CI-Lauf ohne Textauswertung entscheidbar") schwer zu
  vereinbaren. Zu ändern wären die Werte **und** die Schranke in
  `istGefordert`; beides bewertet jeden bisherigen Lauf neu und gehört
  deshalb in einen eigenen Schritt.
* **M0.5** — ungemessen, kein NAS verfügbar.

### 7.8 Sweep nach der dritten Nacharbeit — **15 von 18 bestanden, kein einziger Verlust**

Gefahren am 2026-09-09 gegen den Stand `0b7a632`, achtzehn Startwerte, voller
Abnahmeplan (4 Clients, 2.000 Kommandos, 5 Phasen), frischer Ordner je Lauf,
sechs Läufe gleichzeitig, `dist` **während** des Sweeps unverändert — die
Prüfsumme über die gebauten Dateien war vor und nach dem Sweep dieselbe
(`c9d81161c15f9719debafd29c3c0d455`). Dieselben Startwerte wie in 7.5 und 7.7.

| | 7.5 | 7.7 | **7.8** |
|---|---|---|---|
| Läufe | 18 | 18 | 18 |
| bestanden | 12 | 13 | **15** |
| gefallen | 6 | 5 | **3** |
| roter Ausgang (`abweichend`) | 0 | 0 | **0** |
| Läufe mit verlorenen Ereignissen | 5 | 1 | **0** |

**Alle achtzehn Läufe melden „kein Ereignis verloren: jede geschriebene Zeile
ist auf dem Share auswertbar".** Das gilt zum ersten Mal für den ganzen Sweep.

**999 und 12345 bestehen.** Die beiden Läufe, die 7.7 als Entscheidung 18 und
17 aufbereitet hat:

* **999** fiel mit zwei Phasen „nicht vergleichbar" — der Versionsvektor deckte
  sich nach einer Reparatur nach §4.6 nie wieder. §7.6 nimmt das ersetzte
  Segment jetzt aus dem Vektor (Entscheidung 18, Richtung A); der Lauf führt
  den Konvergenznachweis in allen Phasen.
* **12345** verlor zwei Ereignisse unter dem Präfix einer aufgegebenen Kennung.
  Die Vollprüfung beim Öffnen nimmt diese Dateien wieder auf und repariert
  durch ein Ersatzsegment unter der neuen Kennung (Entscheidung 17, Richtung
  B); der Lauf verliert nichts mehr.

#### Die drei gefallenen Läufe — alle aus demselben, benannten Grund

**2 — `schreibrechtEntzug`. 6 und 20260909 — `profilKlon`.** In allen drei
Fällen ist eine nach §9 geforderte Störung im Lauf nicht eingetreten. Das ist
kein inhaltlicher Befund, sondern **der Würfel**, und es sind exakt dieselben
drei Startwerte wie in 7.7. Über den ganzen Sweep sind das die **einzigen** drei
Mangelzeilen; keine andere Ursache kommt vor.

Damit ist der Punkt aus „Was offen bleibt" jetzt der einzige, der noch Läufe
kostet: Solange `profilKlon` in `ALLE_FEHLER` auf 0,001 je Kommando steht, fällt
rund jeder siebte Lauf allein daran. **Nicht geändert** — jede Änderung an den
Werten bewertet jeden bisherigen Lauf neu und gehört in einen eigenen Schritt
mit eigener Entscheidung.

#### Der Preis von Entscheidung 17, gemessen

Die Vollprüfung beim Öffnen liest seit 17 B auch die Dateien aufgegebener
Kennungen. Über die achtzehn Läufe steht A10 zwischen **19,6 %** und **70,2 %**
des Erstlaufs (7.7 nannte für den Einzellauf 27,6 %). Die Spanne folgt der Zahl
der Kennungswechsel: 43 Wechsel und 140 Reparaturen verteilen sich über den
Sweep, und in sechzehn der achtzehn Läufe kommen beide zusammen vor. Die **Zeit**
dazu misst M0.5 am echten Share; hier steht allein die Datenmenge.
