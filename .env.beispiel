# Vorlage fuer `.env` neben docker-compose.yml — nur fuer Weg B (Docker haengt
# das SMB-Share selbst ein). Nach `.env` kopieren und ausfuellen; `.env` ist
# in .gitignore und wird nicht eingecheckt.
S1_NAS_HOST=nas.fuest.lan
S1_NAS_FREIGABE=S1-Control
S1_NAS_BENUTZER=s1control
S1_NAS_PASSWORT=
