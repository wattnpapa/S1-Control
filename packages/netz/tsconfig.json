{
  // Ring 3 — darf node:dgram, aber kein DOM, kein React, kein Electron.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"],
    "outDir": "dist",
    // Bauzustand in den Ausgabeordner: wer dist/ loescht, loescht ihn mit.
    "tsBuildInfoFile": "dist/.tsbuildinfo",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "references": [{ "path": "../domaene" }]
}
